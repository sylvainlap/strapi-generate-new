'use strict';

/**
 * Expose studio config
 */

module.exports = function dataForStudioJSON(scope) {
  return {
    'studio': {
      'enabled': true,
      'secretKey': 'YOUR SECRET KEY HERE'
    }
  };
};
